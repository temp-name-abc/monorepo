import { Folders, Search, Settings } from "tabler-icons-react";

export const links = [
    { href: "/storage/collections", children: "Collections", icon: <Folders /> },
    { href: "#", children: "Search", icon: <Search /> },
];

export default links;
